@model ModelForCreationDto

<div>
    <h3>Create Model</h3>

    <form method="post" asp-action="Create">
        <div class="changing-field">
            <label asp-for="Name">Name</label>
            <div>
                <input class="simple-input" asp-for="Name"/>
                <span class="error-mes" asp-validation-for="Name"></span>
            </div>
        </div>

        <div class="changing-field">
            <label asp-for="YearOfRelease">Year of release</label>
            <div>
                <input class="simple-input" asp-for="YearOfRelease"/>
                <span class="error-mes" asp-validation-for="YearOfRelease"></span>
            </div>
        </div> 

        <button class="creation-btn" type="submit">Create</button>
    </form>

    <a class="back-btn" href="Index">Back</a>

    @if (ViewData["Message"] != null)
    {
        <h5>@ViewData["Message"]</h5>
    }
</div>